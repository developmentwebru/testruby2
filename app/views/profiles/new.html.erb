<%= content_for :title do %>
  Укажите ваши данные
<% end %>

<h4 class="text-center mb-3">Укажите ваши данные</h4>
<div class="w-100 mx-auto" style="max-width: 350px;">
  <%= simple_form_for @profile, url: profile_path do |f| %>
    <div class="form-group">
      <%= f.input :first_name, label: false, placeholder: "Имя" %>
    </div>

    <div class="form-group">
      <%= f.input :last_name, label: false, placeholder: "Фамилия" %>
    </div>

    <div class="form-group">
      <%= f.input :phone, label: false, placeholder: "Номер мобильного телефона" %>
    </div>

    <div class="form-group">
      <%= f.input :address, label: false, placeholder: "Адрес (улица, дом)" %>
    </div>

    <div class="form-group">
      <%= f.input :city, label: false, placeholder: "Город" %>
    </div>

    Фото паспорта
    <small class="d-block mb-2">
      Фотографии или сканы вашего паспорта (2 стороны) необходимы для прохождения таможенного оформления
    </small>

    <%= render(FileUploadComponent.new(
      form: f,
      model_name: "profile",
      model_instance: @profile,
      nested_attributes_name: "passport_photos",
      max_files: 2
    )) %>

    <%= f.button :submit, "Завершить регистрацию", class: "mt-3 btn-block" %>
  <% end %>
</div>
