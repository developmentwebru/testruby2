<% if resource.errors.any? %>
  <div class="js-alert alert alert-danger" role="alert" id="error_explanation">
    <% resource.errors.full_messages.each do |message| %>
      <div><%= message %>.</div>
    <% end %>
  </div>
<% end %>
